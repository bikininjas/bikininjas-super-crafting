pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url = "https://maven.neoforged.net/releases" }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'mod-super-crafting'

// CI composite build — core-lib checked out alongside via actions/checkout
def coreLibDir = file('core-lib')
if (coreLibDir.exists()) {
    includeBuild(coreLibDir) {
        dependencySubstitution {
            // Match the Maven coordinate used in build.gradle
            substitute module('com.bikininjas.corelib:core_lib') using project(':')
        }
    }
}
